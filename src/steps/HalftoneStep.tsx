export function HalftoneStep() {
  return (
    <section>
      <h2>Step 6 – Halftone</h2>
      <p>
        Placeholder UI for halftone conversion. Later, this will surface the
        engraving-aware halftone suggestions (LPI, angle, shape) from the AI.
      </p>
    </section>
  );
}
